# Example environment variables for WhisperBox backend
# Copy this to .env on the server (do NOT commit real credentials to git)

DB_HOST=127.0.0.1
DB_NAME=whisperbox_db
DB_USER=whisperbox_user
DB_PASS=strong_password_here
DB_CHARSET=utf8mb4

# Optional: set to 'production' in production deployments
APP_ENV=development

# Recommended: random secret for session/csrf token operations (keep private)
APP_SECRET=change_this_to_a_secure_random_value
